import { Fragment } from "react";
import StoreGrid from "../Store/StoreGrid/StoreGrid";

const Store = () => {
  return (
    <Fragment>
      <StoreGrid />
    </Fragment>
  );
};

export default Store;
